# 极简化Nixpacks配置 - 明确指定Python FastAPI项目
[phases.setup]
nixPkgs = ['python3']

[phases.install]
cmds = [
    'python3 -m pip install --upgrade pip',
    'python3 -m pip install -r requirements.txt'
]

[start]
cmd = 'python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT'
